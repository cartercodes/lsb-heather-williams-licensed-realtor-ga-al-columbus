/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/lsb-heather-williams-licensed-realtor-ga-al-columbus',
  images: { unoptimized: true },
};
module.exports = nextConfig;
